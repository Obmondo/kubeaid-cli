{{- /*
NetBird Kubernetes Operator values overlay.

Empty on purpose. The operator deploys with chart defaults; concrete
NetBird wiring (managementURL, API key, NBSetupKey / NBPolicy CRs) is
the operator's responsibility post-bootstrap — same shape as the
deployed vpn-obmondo cluster. The post-prompt notice points the
operator at the two manual steps (setup key + group ACLs).

If/when kubeaid-cli auto-mints API keys via NetBird Mgmt, this file
will start carrying managementURL + netbirdAPI.keyFromSecret.
*/ -}}
