apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  labels:
    kubeaid.io/version: {{ .KubeaidFork.Version }}
    kubeaid.io/managed-by: kubeaid
    kubeaid.io/sync-order: "10"
  name: azure-workload-identity-webhook
  namespace: argocd
spec:
  destination:
    namespace: azure-workload-identity-system
    server: https://kubernetes.default.svc
  sources:
    - repoURL: {{ .KubeaidFork.ParsedURL.GetHttpCloneURL }}
      path: argocd-helm-charts/azure-workload-identity-webhook
      targetRevision: {{ .KubeaidFork.Version }}
      helm:
        valueFiles:
          - $values/k8s/{{ .KubeaidConfigFork.Directory }}/argocd-apps/values-azure-workload-identity-webhook.yaml
    - repoURL: {{ .KubeaidConfigFork.ParsedURL.GetHttpCloneURL }}
      targetRevision: HEAD
      ref: values
  project: kubeaid
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true

